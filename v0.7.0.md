# v0.7.0

## Exec rework
- fluent interface
- pipe support
    ? parse from args
    ? as method
    ? other
- raw string input
    - pipe as arg
    - string replacement
    - closures `()`

## Blume language and Blush
