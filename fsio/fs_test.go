package fsio_test

import (
	"testing"
)

func TestBFS(t *testing.T) {
	// fsio.Find("C:/github.com/", gen.Contains(typ.String("const.go"))).Match(
	// func(s typ.String) { yap.Info("found match with fsio.Find", "match", s) },
	// func(e T.Error[any]) { t.Fatal(e.Error()) },
	// )
}
